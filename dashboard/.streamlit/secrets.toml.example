# Streamlit Community Cloud — copy these as Secrets in the app settings.
# DO NOT commit a real secrets.toml (it is in .gitignore).

# URL publique du backend FastAPI déployé sur Render.
API_URL = "https://obrail-backend.onrender.com"

# Optionnel : laisser vide en prod (Prometheus n'est pas exposé publiquement).
PROMETHEUS_URL = ""
